<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainMenuActivity" >

    <Button
        android:id="@+id/RegisterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="197dp"
        android:text="Register" />

    <Button
        android:id="@+id/SalesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/RegisterButton"
        android:layout_alignBottom="@+id/RegisterButton"
        android:layout_centerHorizontal="true"
        android:text="Sales" />

    <Button
        android:id="@+id/SyncButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/SalesButton"
        android:layout_alignBottom="@+id/SalesButton"
        android:layout_marginLeft="170dp"
        android:layout_toRightOf="@+id/SalesButton"
        android:text="Sync" />

</RelativeLayout>